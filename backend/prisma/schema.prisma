// This is the Prisma schema for FabricGen.
// No models are defined yet. This file serves as the foundation
// for future database schema development.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}